name: CC Docker Image Build

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_PASSWORD }}

    - name: Build and push Central Docker image
      uses: docker/build-push-action@v3
      with:
        context: ./central
        file: ./central/Dockerfile
        push: true
        tags: stanisica/cc-docker-images:central+${{ github.run_id }}
    
    - name: Build and push City Docker image
      uses: docker/build-push-action@v3
      with:
        context: ./city
        file: ./city/Dockerfile
        push: true
        tags: stanisica/cc-docker-images:city+${{ github.run_id }}


